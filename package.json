{
  "scripts": {
    "build": "webpack && npm run copy",
    "copy": "cp -fv ./img/*.png ./dist/ && cp -fv ./manifest.json ./dist/"
  },
  "devDependencies": {
    "webpack": "^5.83.1",
    "webpack-cli": "^5.1.1"
  }
}
